The repository contains an example of a Python test script.
The script uses such libraries as `pytest` and `playwright` to demonstrate my skills in Python.
The aim of it all is just to practice in writing a Python test 'cause it's fun.

***

Репозиторий содержит пример тестового скрипта на Python.
В скрипте используются такие библиотеки, как `pytest` и `playwright`, чтобы продемонстрировать мои навыки работы с Python.
Цель всего этого – попрактиковаться в написании тестов на Python (потому что это интересно).
